/**
 * Stable alias for the root site config, so route files can import it
 * from any depth without fragile relative paths.
 */
export { site } from "../../site.config";
export type { Site } from "../../site.config";
